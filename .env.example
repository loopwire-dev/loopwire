# Loopwire environment variables
# Copy to .env and adjust as needed.

# Required by daemon: frontend origin used for CORS and connect links.
# LOOPWIRE_FRONTEND_URL=http://localhost:5173

# Optional: additional CORS origins (comma-separated).
# LOOPWIRE_ALLOWED_ORIGINS=http://localhost:4173,https://loopwire.dev

# Optional (web): force daemon base URL in the frontend.
# If unset, web uses discovery/query/session fallback.
# VITE_DAEMON_URL=http://localhost:9400
