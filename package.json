{
  "name": "loopwire",
  "private": true,
  "scripts": {
    "prek:install": "prek install --hook-type pre-commit --hook-type pre-push",
    "prek:run": "prek run --all-files && prek run --hook-stage pre-push --all-files"
  },
  "workspaces": [
    "apps/web",
    "packages/types"
  ]
}
