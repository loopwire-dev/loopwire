# Apps

This directory contains the two main Loopwire applications:

- `web/`: Browser UI.
- `daemon/`: Local daemon (`loopwired`) that powers the platform.

Typical local development uses both at the same time.

## Run

From repo root:

```bash
moon run daemon:dev
moon run web:dev
```
